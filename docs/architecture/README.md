# Architecture Notes

This directory stores architecture decisions, diagrams, and package boundaries.

Initial direction:
- Flutter-first cross-platform client
- Modular monorepo-style layout via `apps/` + `packages/`
- Domain and product scope to be constrained by PRD once available
